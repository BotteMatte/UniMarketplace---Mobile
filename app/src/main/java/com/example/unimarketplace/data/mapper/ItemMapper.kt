package com.example.unimarketplace.data.mapper

/**
 * ItemMapper: Utility per la conversione dei dati.
 * Converte le entità del database (Room Entities) in modelli di dominio (Domain Models)
 * e viceversa, per mantenere separati i livelli dell'architettura.
 */
object ItemMapper {
    // fun toDomain(entity: ItemEntity): Item { ... }
}
