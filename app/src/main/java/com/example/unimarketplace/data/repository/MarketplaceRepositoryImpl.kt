package com.example.unimarketplace.data.repository

/**
 * MarketplaceRepositoryImpl: Implementazione concreta del repository.
 * Gestisce il flusso di dati tra il database locale (Room) e la UI, 
 * fornendo i metodi per recuperare, inserire e filtrare gli annunci.
 */
class MarketplaceRepositoryImpl {
    // Implementazione dei metodi definiti nel domain repository
}
