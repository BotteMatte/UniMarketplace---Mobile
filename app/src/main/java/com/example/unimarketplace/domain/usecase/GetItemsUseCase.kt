package com.example.unimarketplace.domain.usecase

/**
 * GetItemsUseCase: Caso d'uso per il recupero degli annunci.
 * Contiene la logica di business specifica per filtrare e ordinare gli articoli
 * della bacheca prima che vengano visualizzati dall'utente.
 */
class GetItemsUseCase {
    // operator fun invoke(): Flow<List<Item>> { ... }
}
