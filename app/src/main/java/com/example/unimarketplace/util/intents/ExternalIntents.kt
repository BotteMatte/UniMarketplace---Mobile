package com.example.unimarketplace.util.intents

/**
 * ExternalIntents: Interazione con applicazioni esterne.
 * Fornisce metodi helper per lanciare Intent esterni come l'invio di messaggi WhatsApp,
 * email al venditore o l'aggiunta di appuntamenti al Calendario.
 */
object ExternalIntents {
    // fun openWhatsApp(phoneNumber: String) { ... }
    // fun sendEmail(address: String) { ... }
}
